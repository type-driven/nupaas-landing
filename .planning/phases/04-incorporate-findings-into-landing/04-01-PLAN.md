---
phase: 04-incorporate-findings-into-landing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Personas.tsx
autonomous: true
requirements: [CLAIM-10, CLAIM-11]

must_haves:
  truths:
    - "Personas bento stat card reads 'Up to 80%' (not bare '80%')"
    - "Bootstrapped startups persona body says 'can cut your infra bill by up to 80%' — not 'cuts your infra bill by 80%'"
  artifacts:
    - path: "src/components/Personas.tsx"
      provides: "Qualified 'Up to 80%' stat value and softened bootstrapped startups body copy"
      contains: "Up to 80%"
  key_links:
    - from: "src/components/Personas.tsx"
      to: "bento-stat-value div (row 1, col 1)"
      via: "bento-stat-value className"
      pattern: "Up to 80%"
    - from: "src/components/Personas.tsx"
      to: "bootstrapped startups bento-persona card"
      via: "bento-wide bento-persona h3 Bootstrapped startups"
      pattern: "up to 80%"
---

<objective>
Apply the two remaining unqualified "80%" fixes from CLAIM-AUDIT.md to Personas.tsx.

Purpose: Phases 1-3 addressed every flagged claim except two in Personas.tsx — the bento stat card showing a bare "80%" and the bootstrapped startups persona body asserting an absolute "cuts your infra bill by 80%". These are the last defensibility gaps identified in the claim audit.

Output:
- Personas.tsx with both "80%" instances qualified as "Up to 80%" / "up to 80%"
</objective>

<execution_context>
@/Users/davidpeter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpeter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/CLAIM-AUDIT.md
</context>

<interfaces>
<!-- Current Personas.tsx bento stat card (row 1, col 1) — lines 11–15 -->
```tsx
<div className="bento-card bento-stat bento-accent-10">
  <div className="bento-stat-value">80%</div>
  <div className="bento-stat-label">lower infra costs</div>
</div>
```

<!-- Current bootstrapped startups persona card (row 1, col 3) — lines 17–26 -->
```tsx
<div className="bento-card bento-wide bento-persona">
  <h3>Bootstrapped startups</h3>
  <p>
    {"You're watching every euro. NuPaas cuts your infra bill by 80% so you can extend your runway and focus on product."}
  </p>
</div>
```

<!-- CLAIM-AUDIT.md item 10 replacement for bento-stat-value: -->
<!-- "Up to 80%" -->

<!-- CLAIM-AUDIT.md item 11 replacement for bootstrapped startups body: -->
<!-- "NuPaas can cut your infra bill by up to 80%, helping you extend runway and stay focused on product." -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Qualify "80%" claims in Personas bento (CLAIM-10, CLAIM-11)</name>
  <files>src/components/Personas.tsx</files>
  <action>
Make exactly two changes to src/components/Personas.tsx. Do not touch any other content.

**Change 1 — Stat card value (CLAIM-10):**
Find the bento-stat-value div that reads `80%` (row 1, first card). Change its text content from `80%` to `Up to 80%`. The label "lower infra costs" stays unchanged.

Before:
```tsx
<div className="bento-stat-value">80%</div>
```
After:
```tsx
<div className="bento-stat-value">Up to 80%</div>
```

**Change 2 — Bootstrapped startups persona body (CLAIM-11):**
Find the bootstrapped startups bento-wide bento-persona card. Replace the body `<p>` content with the CLAIM-AUDIT.md replacement copy:

Before:
```tsx
{"You're watching every euro. NuPaas cuts your infra bill by 80% so you can extend your runway and focus on product."}
```
After:
```tsx
{"You're watching every euro. NuPaas can cut your infra bill by up to 80%, helping you extend runway and stay focused on product."}
```

Do not change the JSX structure, className attributes, or any other cards. Do not reformat or reorder lines.
  </action>
  <verify>
    <automated>grep -n "bento-stat-value" /Users/davidpeter/workspace/type-driven.com/nupaas-landing/src/components/Personas.tsx | grep -v "Up to 80%" | grep "80%" && echo "FAIL: unqualified 80% still in stat card" || echo "PASS: stat card qualified"</automated>
    <automated>grep -n "Up to 80%" /Users/davidpeter/workspace/type-driven.com/nupaas-landing/src/components/Personas.tsx && echo "PASS: Up to 80% present"</automated>
    <automated>grep -n "up to 80%" /Users/davidpeter/workspace/type-driven.com/nupaas-landing/src/components/Personas.tsx && echo "PASS: up to 80% in persona body"</automated>
    <automated>grep -n "cuts your infra bill by 80%" /Users/davidpeter/workspace/type-driven.com/nupaas-landing/src/components/Personas.tsx && echo "FAIL: old unqualified copy still present" || echo "PASS: old copy removed"</automated>
    <automated>cd /Users/davidpeter/workspace/type-driven.com/nupaas-landing && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Bento stat card reads "Up to 80%" (not "80%")
    - Bootstrapped startups body reads "can cut your infra bill by up to 80%, helping you extend runway and stay focused on product."
    - No bare "80%" claim remains in Personas.tsx (only "Up to 80%" and "up to 80%" forms)
    - `npm run build` exits 0 with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
After the task is complete:

1. Run `npm run build` from project root — must exit 0.
2. Confirm zero bare "80%" in Personas.tsx: `grep -n '"80%"' src/components/Personas.tsx` must return no matches.
3. Confirm "Up to 80%" appears in the stat card and "up to 80%" appears in the bootstrapped startups persona body.
</verification>

<success_criteria>
- CLAIM-10: Personas bento stat-value reads "Up to 80%" — the unqualified absolute stat is gone
- CLAIM-11: Bootstrapped startups persona body uses "can cut your infra bill by up to 80%" framing
- All prior claim fixes from phases 1-3 remain intact (no regressions)
- Build passes: `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-incorporate-findings-into-landing/04-01-SUMMARY.md` using the summary template at `@/Users/davidpeter/.claude/get-shit-done/templates/summary.md`.
</output>
