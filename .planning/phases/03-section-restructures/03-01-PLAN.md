---
phase: 03-section-restructures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Features.tsx
  - src/styles/features.css
  - src/components/HowItWorks.tsx
  - src/components/Personas.tsx
  - src/styles/personas.css
autonomous: true
requirements: [FEAT-01, FEAT-02, FEAT-03, FEAT-04, FEAT-05, FEAT-06, HOW-01, HOW-02, PER-01, PER-02]

must_haves:
  truths:
    - "Features section shows exactly five pillar cards: Ship, Operate, Secure, Observe, Migrate"
    - "Migrate pillar names Supabase Lift and Vercel Lift explicitly"
    - "HowItWorks heading reads 'Provision, ship, and operate — in one afternoon'"
    - "HowItWorks step labels are Day 0 / Day 1 / Day 2 (not 01 / 02 / 03)"
    - "HowItWorks step titles and body match the DAY0-DAY1-DAY2.md research copy verbatim"
    - "Personas bento shows five persona cards total — original three plus two new ones"
    - "New persona cards match the bento-wide bento-persona pattern of the existing three"
  artifacts:
    - path: "src/components/Features.tsx"
      provides: "5-pillar features component"
      contains: "pillar"
    - path: "src/styles/features.css"
      provides: "Pillar card grid styles"
      contains: "pillar"
    - path: "src/components/HowItWorks.tsx"
      provides: "Day 0/1/2 ops readiness steps"
      contains: "Day 0"
    - path: "src/components/Personas.tsx"
      provides: "5-card bento grid with new personas"
      contains: "Vercel/Supabase"
    - path: "src/styles/personas.css"
      provides: "Updated bento grid layout for 5 persona rows"
      contains: "nth-child(11)"
  key_links:
    - from: "src/components/Features.tsx"
      to: "src/styles/features.css"
      via: "import"
      pattern: "import.*features\\.css"
    - from: "src/components/Personas.tsx"
      to: "src/styles/personas.css"
      via: "import"
      pattern: "import.*personas\\.css"
---

<objective>
Restructure Features, HowItWorks, and Personas to match the marketing handoff doc's product framing. Features becomes a 5-pillar card grid (Ship / Operate / Secure / Observe / Migrate). HowItWorks adopts the Day 0 / Day 1 / Day 2 ops-readiness framing with copy from the research doc. Personas adds two new bento cards for the migration and DevOps/SRE ICPs while preserving the existing three.

Purpose: The current Features list is a flat 10-item grid with no product structure. HowItWorks frames the product as a quick-start tool, not an ops platform. These gaps misrepresent the product to the European enterprise ICPs the page targets.

Output: Three updated TSX components and two updated CSS files. No new dependencies. No visual redesign — only structural and copy changes within the existing design system.
</objective>

<execution_context>
@/Users/davidpeter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpeter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/DAY0-DAY1-DAY2.md

<interfaces>
<!-- Current component structures. Executor should replace data arrays in full — no partial edits. -->

From src/components/Features.tsx (CURRENT — replace entirely):
```tsx
// Data shape: { name: string; detail: string }[]
// Grid class: features-grid
// Item class: feature-item
// Uses: feature-dot, feature-name, feature-detail
```

From src/styles/features.css (CURRENT):
```css
/* Grid: 1fr 1fr, gap: 1px, background: var(--border-subtle) */
/* Items: feature-item, feature-dot, feature-name, feature-detail */
```

From src/components/HowItWorks.tsx (CURRENT — replace data array entirely):
```tsx
// Data shape: { number: string; title: string; body: string }[]
// Grid class: steps-grid (3-col repeat)
// Item class: step-card
// Number element class: step-number
```

From src/components/Personas.tsx (CURRENT bento structure):
```
Row 1: [stat bento-accent-10] [stat bento-accent-15] [persona bento-wide]
Row 2: [persona bento-wide] [stat bento-accent-08] [stat bento-accent-12]
Row 3: [stat bento-accent-06] [stat bento-accent-10] [persona bento-wide]
```
Grid: 1fr 1fr 2fr (3 columns, persona cards span 2 via bento-wide)
CSS nth-child selectors: 4th card gets grid-column 1/3, 9th card gets grid-column 3/5.

From src/styles/personas.css (CURRENT nth-child rules):
```css
.bento-card:nth-child(4) { grid-column: 1 / 3; }
.bento-card:nth-child(7), .bento-card:nth-child(8) { grid-column: auto; }
.bento-card:nth-child(9) { grid-column: 3 / 5; }
```

New bento-accent variants available (all in personas.css already):
bento-accent-06, bento-accent-08, bento-accent-10, bento-accent-12, bento-accent-15
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure Features to 5-pillar card grid</name>
  <files>src/components/Features.tsx, src/styles/features.css</files>
  <action>
Replace Features.tsx with a 5-pillar card layout. The data shape changes from a flat feature list to an array of pillars, each with a name, icon character, and feature list array. Replace the features-grid CSS to a 5-column layout for pillars and add pillar card sub-element styles.

**New data shape for Features.tsx:**

```tsx
type Pillar = {
  name: string;
  icon: string;
  features: string[];
};

const pillars: Pillar[] = [
  {
    name: "Ship",
    icon: "⚡",
    features: [
      "Project lifecycle management",
      "Git-push deploys with build logs",
      "Deployment history and rollback",
      "Preview environments with TTL cleanup",
      "Custom domains, DNS, and SSL",
    ],
  },
  {
    name: "Operate",
    icon: "⚙️",
    features: [
      "Node lifecycle management",
      "Node groups and auto-scaling",
      "Multi-cloud provisioning and sync",
      "Auto-onboarding for new nodes",
      "Stack deploys across providers",
    ],
  },
  {
    name: "Secure",
    icon: "🔒",
    features: [
      "Org, member, invite, and access-key lifecycle",
      "Credential verification",
      "SSH key lifecycle",
      "Vault-backed secrets management",
      "Audit logs",
    ],
  },
  {
    name: "Observe",
    icon: "📊",
    features: [
      "Live status dashboard with SSE stream",
      "Structured log and trace explorers",
      "Dashboards and saved searches",
      "Configurable alerts",
    ],
  },
  {
    name: "Migrate",
    icon: "🚀",
    features: [
      "Source control integrations",
      "Domain and DNS migration flows",
      "Supabase Lift",
      "Vercel Lift",
    ],
  },
];
```

**New JSX for Features.tsx:**

```tsx
import { SectionWrapper } from "./ui/SectionWrapper.tsx";
import "../styles/features.css";

type Pillar = {
  name: string;
  icon: string;
  features: string[];
};

const pillars: Pillar[] = [ /* array above */ ];

export function Features() {
  return (
    <SectionWrapper alt id="features">
      <h2 className="section-heading">
        Everything you need. Nothing you don't.
      </h2>
      <div className="features-pillars reveal-children">
        {pillars.map((pillar) => (
          <div key={pillar.name} className="pillar-card">
            <div className="pillar-header">
              <span className="pillar-icon" aria-hidden="true">{pillar.icon}</span>
              <h3 className="pillar-name">{pillar.name}</h3>
            </div>
            <ul className="pillar-features">
              {pillar.features.map((f) => (
                <li key={f} className="pillar-feature-item">
                  <span className="pillar-dot" aria-hidden="true" />
                  {f}
                </li>
              ))}
            </ul>
          </div>
        ))}
      </div>
    </SectionWrapper>
  );
}
```

**New features.css** — replace the file entirely. Keep existing variable references (--border-subtle, --bg-primary, --bg-section-alt, --accent, --card-radius, --transition-fast, --font-heading, --body-small, --text-primary, --text-secondary, --card-padding). Remove all old .features-grid/.feature-item/.feature-dot/.feature-name/.feature-detail rules. Add:

```css
.features-pillars {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 1px;
  background: var(--border-subtle);
  border: 1px solid var(--border-subtle);
  border-radius: var(--card-radius);
  overflow: hidden;
}

.pillar-card {
  background: var(--bg-primary);
  padding: 2rem 1.5rem;
  transition: background var(--transition-fast);
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
}

.pillar-card:hover {
  background: var(--bg-section-alt);
}

.pillar-header {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 0.5rem;
}

.pillar-icon {
  font-size: 1.5rem;
  line-height: 1;
}

.pillar-name {
  font-family: var(--font-heading);
  font-size: 1.125rem;
  font-weight: 700;
  color: var(--text-primary);
  margin: 0;
}

.pillar-features {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 0.625rem;
}

.pillar-feature-item {
  display: flex;
  align-items: flex-start;
  gap: 0.625rem;
  font-size: var(--body-small);
  color: var(--text-secondary);
  line-height: 1.5;
}

.pillar-dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--accent);
  flex-shrink: 0;
  margin-top: 0.45rem;
}

@media (max-width: 1024px) {
  .features-pillars {
    grid-template-columns: repeat(3, 1fr);
  }
}

@media (max-width: 640px) {
  .features-pillars {
    grid-template-columns: 1fr 1fr;
  }
}

@media (max-width: 400px) {
  .features-pillars {
    grid-template-columns: 1fr;
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/davidpeter/workspace/type-driven.com/nupaas-landing && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Features.tsx exports Features(), renders 5 pillar-card divs inside features-pillars
    - Pillar names are exactly: Ship, Operate, Secure, Observe, Migrate
    - Migrate pillar features list includes the strings "Supabase Lift" and "Vercel Lift"
    - features.css has no .features-grid or .feature-item rules (old classes removed)
    - features.css has .features-pillars with grid-template-columns: repeat(5, 1fr)
    - TypeScript compiles without errors in these files
  </done>
</task>

<task type="auto">
  <name>Task 2: Reframe HowItWorks as Day 0 / Day 1 / Day 2</name>
  <files>src/components/HowItWorks.tsx</files>
  <action>
Replace HowItWorks.tsx data array and heading. The CSS (how-it-works.css) does NOT change — the step-card layout, step-number styling, and responsive rules stay exactly as-is. Only the TSX file changes.

**New steps array and heading — copy verbatim from DAY0-DAY1-DAY2.md research:**

```tsx
const steps = [
  {
    number: "Day 0",
    title: "Infrastructure ready before your first deploy",
    body: "Connect your Hetzner, Scaleway, or AWS credentials and NuPaas provisions the full stack: K3s cluster, WireGuard mesh, Traefik ingress, and container registry — no YAML authoring required. Node lifecycle, cloud connector sync, and auto-onboarding are handled from the start, so your team inherits a production-grade foundation rather than building one.",
  },
  {
    number: "Day 1",
    title: "Git-push deploys with secrets from day one",
    body: "Connect a Git repository and every push triggers a build, containerization, and deploy with a live URL — pull requests get isolated preview environments with automatic TTL cleanup so staging debt does not accumulate. Vault-backed secrets, domain and DNS configuration, and SSL provisioning are available on the first deploy, not retrofitted later.",
  },
  {
    number: "Day 2",
    title: "Observe, audit, and recover without heroics",
    body: "A live status dashboard with SSE streaming, structured log and trace explorers, saved searches, and configurable alerts give your team visibility without assembling a monitoring stack from scratch. Credential rotation, SSH key lifecycle, audit logs, durable background workers, and rollout controls by role mean that the operational controls your on-call team needs are already in place when incidents happen.",
  },
];
```

**New section heading:**
```tsx
<h2 className="section-heading">
  Provision, ship, and operate — in one afternoon.
</h2>
```

The full component structure (SectionWrapper, steps-grid, step-card, step-number, h3, p) stays identical to the current implementation — only the heading string and steps array data change. The `number` field drives the `.step-number` div which previously held "01"/"02"/"03" — it now holds "Day 0"/"Day 1"/"Day 2". The step-number CSS class uses font-size: 3rem and color: var(--accent) — "Day 0" will render slightly wider than "01" but fits within the card. No CSS change is needed.
  </action>
  <verify>
    <automated>cd /Users/davidpeter/workspace/type-driven.com/nupaas-landing && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - HowItWorks.tsx heading reads "Provision, ship, and operate — in one afternoon."
    - steps[0].number === "Day 0", steps[1].number === "Day 1", steps[2].number === "Day 2"
    - steps[0].title === "Infrastructure ready before your first deploy"
    - steps[2].title === "Observe, audit, and recover without heroics"
    - how-it-works.css is unchanged (no diff on that file)
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Add two new persona cards to Personas bento</name>
  <files>src/components/Personas.tsx, src/styles/personas.css</files>
  <action>
Add two new persona cards to Personas.tsx and update personas.css nth-child selectors to account for the new total of 11 cards (9 existing + 2 new persona cards). The two new personas are appended as rows 4 and 5 in the bento.

**Current bento: 9 cards across 3 rows.**
After adding 2 new persona cards (each bento-wide spanning 2 of 3 columns), rows 4 and 5 each need one stat card + one wide persona card. Add one stat card per new persona to keep the bento rhythm.

**New row 4 (2 new cards after existing row 3):**
- Card 10: stat card — value: "0" / label: "rearchitecting required" (bento-accent-08)
- Card 11: persona card (bento-wide) — "Teams migrating from Vercel/Supabase"

**New row 5 (2 more cards):**
- Card 12: persona card (bento-wide) — "Platform / DevOps / SRE teams"
- Card 13: stat card — value: "1" / label: "control plane" (bento-accent-12)

Wait — the 3-column grid (1fr 1fr 2fr) means each row fits exactly 3 cells. A bento-wide card spans 2 columns. So each row that contains one wide persona + one stat uses exactly 3 column-cells. This matches the pattern of existing rows 1 and 3.

**New layout plan (all 13 cards):**
```
Row 1 (cards 1-3):  [stat]       [stat]       [persona bento-wide]
Row 2 (cards 4-6):  [persona bento-wide]  [stat]  [stat]
Row 3 (cards 7-9):  [stat]       [stat]       [persona bento-wide]
Row 4 (cards 10-11): [stat]      [persona bento-wide]
Row 5 (cards 12-13): [persona bento-wide]  [stat]
```

**New cards to append inside .personas-bento after card 9:**

```tsx
{/* Row 4 */}
<div className="bento-card bento-stat bento-accent-08">
  <div className="bento-stat-value">0</div>
  <div className="bento-stat-label">rearchitecting required</div>
</div>
<div className="bento-card bento-wide bento-persona">
  <h3>Teams migrating from Vercel / Supabase</h3>
  <p>
    {"You\u2019ve hit Vercel\u2019s limits or Supabase\u2019s pricing inflection. NuPaas\u2019s Vercel Lift and Supabase Lift move your projects and databases without a ground-up replatform."}
  </p>
</div>

{/* Row 5 */}
<div className="bento-card bento-wide bento-persona">
  <h3>Platform / DevOps / SRE teams</h3>
  <p>
    {"You operate mixed infrastructure across cloud providers and self-hosted nodes. NuPaas gives you a single control plane for node lifecycle, credential rotation, audit logs, and rollout controls \u2014 without replacing your existing providers."}
  </p>
</div>
<div className="bento-card bento-stat bento-accent-12">
  <div className="bento-stat-value">1</div>
  <div className="bento-stat-label">control plane</div>
</div>
```

**Updated personas.css nth-child rules:**

The existing nth-child rules target specific card positions to override grid-column defaults. With 13 total cards the rules needed are:

- Card 4 (row 2 persona, spans left 2 cols): `grid-column: 1 / 3` — KEEP AS IS
- Card 9 (row 3 persona, spans right 2 cols): `grid-column: 3 / 5` — KEEP AS IS (3-col grid, so column 3 to end)
- Card 11 (row 4 persona, spans right 2 cols): `grid-column: 2 / 4` — NEW
- Card 12 (row 5 persona, spans left 2 cols): `grid-column: 1 / 3` — NEW

Replace the existing nth-child block (lines targeting 4, 5, 6, 7, 8, 9) with:

```css
/* Row 2: wide persona spans left 2 cols */
.bento-card:nth-child(4) {
  grid-column: 1 / 3;
}

.bento-card:nth-child(5),
.bento-card:nth-child(6) {
  grid-column: auto;
}

/* Row 3: two stat cards then wide persona on right */
.bento-card:nth-child(7),
.bento-card:nth-child(8) {
  grid-column: auto;
}

.bento-card:nth-child(9) {
  grid-column: 3 / 5;
}

/* Row 4: stat card then wide persona spans right 2 cols */
.bento-card:nth-child(10) {
  grid-column: auto;
}

.bento-card:nth-child(11) {
  grid-column: 2 / 4;
}

/* Row 5: wide persona spans left 2 cols, then stat card */
.bento-card:nth-child(12) {
  grid-column: 1 / 3;
}

.bento-card:nth-child(13) {
  grid-column: auto;
}
```

Also update the mobile (max-width: 768px) nth-child reset to include the new wide cards:

```css
@media (max-width: 768px) {
  .personas-bento {
    grid-template-columns: 1fr 1fr;
  }

  .bento-wide,
  .bento-card:nth-child(4),
  .bento-card:nth-child(9),
  .bento-card:nth-child(11),
  .bento-card:nth-child(12) {
    grid-column: 1 / -1;
  }

  .bento-stat-value {
    font-size: 2rem;
  }
}

@media (max-width: 480px) {
  .personas-bento {
    grid-template-columns: 1fr;
  }

  .bento-wide,
  .bento-card:nth-child(4),
  .bento-card:nth-child(9),
  .bento-card:nth-child(11),
  .bento-card:nth-child(12) {
    grid-column: auto;
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/davidpeter/workspace/type-driven.com/nupaas-landing && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Personas.tsx contains 13 bento-card divs total (9 original + 4 new)
    - Two new persona cards exist with headings "Teams migrating from Vercel / Supabase" and "Platform / DevOps / SRE teams"
    - Original three persona headings are unchanged: "Bootstrapped startups", "B2B SaaS selling to EU enterprises", "Growing teams (5–20 engineers)"
    - personas.css has nth-child rules for cards 11 and 12 (new rows)
    - personas.css mobile rules include nth-child(11) and nth-child(12) in the grid-column reset
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, verify the overall phase:

1. Run TypeScript compile: `npx tsc --noEmit` — must produce no errors
2. Run dev server: `npm run dev` and open http://localhost:5173
3. Confirm Features section shows 5 named pillars in a horizontal card grid
4. Confirm "Supabase Lift" and "Vercel Lift" appear as text in the Migrate pillar
5. Confirm HowItWorks shows "Day 0", "Day 1", "Day 2" as step labels (not "01", "02", "03")
6. Confirm HowItWorks heading is "Provision, ship, and operate — in one afternoon."
7. Confirm Personas bento shows 5 persona cards including "Teams migrating from Vercel / Supabase" and "Platform / DevOps / SRE teams"
8. Confirm the original 3 persona cards are still present
</verification>

<success_criteria>
- `npx tsc --noEmit` exits 0
- Features grid renders 5 pillar cards: Ship, Operate, Secure, Observe, Migrate
- Migrate pillar text includes "Supabase Lift" and "Vercel Lift"
- HowItWorks step labels read "Day 0", "Day 1", "Day 2"
- HowItWorks section heading reads "Provision, ship, and operate — in one afternoon."
- Personas bento has exactly 5 persona (bento-persona) cards
- All requirements FEAT-01 through FEAT-06, HOW-01, HOW-02, PER-01, PER-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-section-restructures/03-01-SUMMARY.md` using the summary template at @/Users/davidpeter/.claude/get-shit-done/templates/summary.md.
</output>
