---
phase: 02-trust-signals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Sovereignty.tsx
  - src/components/SocialProof.tsx
  - src/styles/social-proof.css
autonomous: true
requirements: [SOV-01, SOV-02, SOV-03, SOV-04, TRUST-01]

must_haves:
  truths:
    - "Sovereignty card 3 heading reads 'Designed for GDPR-conscious teams, Gaia-X aligned'"
    - "Sovereignty card 3 body uses 'audit-friendly controls' language and does not claim Gaia-X credential qualification"
    - "No SOC 2 ready badge exists anywhere in the Sovereignty section"
    - "Gaia-X badge label reads 'Gaia-X aligned' (not 'Gaia-X' or 'Gaia-X ready')"
    - "SocialProof section shows four trust talking-point cards above the existing logos strip"
    - "Trust talking points cover: workflow reliability, unified model, credential lifecycle, rollout controls"
  artifacts:
    - path: "src/components/Sovereignty.tsx"
      provides: "Updated card 3 heading, body copy, and badge set (SOC 2 badge removed, Gaia-X badge relabelled)"
    - path: "src/components/SocialProof.tsx"
      provides: "New trust-points row rendered above the logos strip"
    - path: "src/styles/social-proof.css"
      provides: "CSS classes for the trust-points grid layout"
  key_links:
    - from: "src/components/SocialProof.tsx"
      to: "src/styles/social-proof.css"
      via: "className trust-points-grid / trust-point / trust-point-label / trust-point-body"
      pattern: "trust-point"
    - from: "src/components/Sovereignty.tsx"
      to: "sovereignty-badge-gaiax"
      via: "className sovereignty-badge-gaiax on the Gaia-X badge div"
      pattern: "sovereignty-badge-gaiax"
---

<objective>
Remove unvalidated compliance claims from the Sovereignty section and expand the SocialProof component with four trust talking points above the logos strip.

Purpose: The landing page must not make unqualified certification claims ("SOC 2 ready", "Gaia-X ready") that could mislead prospects or create legal exposure. Separately, the SocialProof strip is a low-scroll section that currently only signals tech stack — adding four trust talking points here converts it into a substantive credibility signal before visitors reach the bottom of the page.

Output:
- Sovereignty.tsx with corrected card 3 heading, body copy, and badge set
- SocialProof.tsx with a new trust-points row above the logos strip
- social-proof.css with grid classes for the trust-points row
</objective>

<execution_context>
@/Users/davidpeter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpeter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/CLAIM-AUDIT.md
</context>

<interfaces>
<!-- Key interfaces extracted from the existing codebase. Use these directly. -->

From src/components/Sovereignty.tsx (current card 3 — full block to be replaced):
```tsx
{/* Card 3: Compliance & Gaia-X — lines 64–100 */}
<div className="sovereignty-card">
  <div className="sovereignty-card-icon"> ... </div>
  <h3>GDPR & Gaia-X ready</h3>           {/* SOV-01: change heading */}
  <p>
    GDPR compliance is the architecture, not a checkbox. NuPaas
    deployments are designed to qualify for a Gaia-X Self-Description
    credential — the EU trust framework for sovereign cloud services.
  </p>                                     {/* SOV-02: rewrite body */}
  <div className="sovereignty-badges">
    <div className="sovereignty-badge"> ... GDPR </div>
    <div className="sovereignty-badge sovereignty-badge-gaiax"> ... Gaia-X </div>
    <div className="sovereignty-badge"> ... SOC 2 ready </div>  {/* SOV-03: remove */}
  </div>
</div>
```

From src/styles/sovereignty.css:
```css
.sovereignty-badge        { /* pill — inline-flex, border, border-radius 8px */ }
.sovereignty-badge-gaiax  { color: #0033a0; border-color: rgba(0,51,160,0.2); background: rgba(0,51,160,0.05); }
```

From src/components/SocialProof.tsx (current outer shell):
```tsx
export function SocialProof() {
  return (
    <section className="social-proof">
      <div className="container social-proof-inner">
        {/* logos strip — two groups with social-proof-group, social-proof-label, social-proof-items */}
      </div>
    </section>
  );
}
```

From src/styles/social-proof.css (existing layout classes in scope):
```css
.social-proof          { padding: 2.5rem 0; background: var(--bg-section-alt); ... }
.social-proof-inner    { display: flex; align-items: center; justify-content: center; gap: 1.5rem; flex-wrap: wrap; }
.social-proof-label    { font-family: var(--font-heading); font-size: var(--body-small); font-weight: 600; color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.08em; }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Sovereignty section — fix card 3 copy and badges (SOV-01 SOV-02 SOV-03 SOV-04)</name>
  <files>src/components/Sovereignty.tsx</files>
  <action>
Replace card 3 (the "GDPR & Gaia-X ready" card, lines 64–100) with the following changes. Do not touch cards 1 or 2.

**Heading (SOV-01):**
Change `GDPR &amp; Gaia-X ready` to `Designed for GDPR-conscious teams, Gaia-X aligned`

**Body copy (SOV-02):**
Replace the existing `<p>` with:
```
GDPR compliance is baked into the architecture, not bolted on as a checkbox. NuPaas is designed for teams with GDPR-conscious data handling requirements, with audit-friendly controls and deployment patterns that align with the Gaia-X trust framework for sovereign cloud services.
```

**Badges (SOV-03 + SOV-04):**
- Remove the third `<div className="sovereignty-badge">` block entirely (the one containing the lock SVG and the text "SOC 2 ready").
- Keep the GDPR badge as-is.
- On the Gaia-X badge (`sovereignty-badge-gaiax`), change the text content from `Gaia-X` to `Gaia-X aligned`. Keep the SVG and className unchanged.

The resulting badge block should contain exactly two badges: GDPR and Gaia-X aligned.
  </action>
  <verify>
    <automated>grep -n "SOC 2" /Users/davidpeter/workspace/type-driven.com/nupaas-landing/src/components/Sovereignty.tsx | wc -l | xargs -I{} test {} -eq 0 &amp;&amp; echo "PASS: SOC 2 badge absent" || echo "FAIL: SOC 2 badge still present"</automated>
    <automated>grep -n "GDPR-conscious" /Users/davidpeter/workspace/type-driven.com/nupaas-landing/src/components/Sovereignty.tsx &amp;&amp; echo "PASS: new heading present"</automated>
    <automated>grep -n "Gaia-X aligned" /Users/davidpeter/workspace/type-driven.com/nupaas-landing/src/components/Sovereignty.tsx &amp;&amp; echo "PASS: Gaia-X aligned label present"</automated>
    <automated>grep -n "audit-friendly" /Users/davidpeter/workspace/type-driven.com/nupaas-landing/src/components/Sovereignty.tsx &amp;&amp; echo "PASS: audit-friendly controls copy present"</automated>
    <automated>cd /Users/davidpeter/workspace/type-driven.com/nupaas-landing &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -20</automated>
  </verify>
  <done>
    - Card 3 heading reads "Designed for GDPR-conscious teams, Gaia-X aligned"
    - Card 3 body contains "audit-friendly controls" and "align with the Gaia-X trust framework" — no Gaia-X credential qualification language
    - Exactly two badges remain: GDPR and "Gaia-X aligned"
    - No "SOC 2 ready" text exists anywhere in the file
    - TypeScript compiles with no errors introduced by this file
  </done>
</task>

<task type="auto">
  <name>Task 2: SocialProof — add trust talking-points row above logos (TRUST-01)</name>
  <files>src/components/SocialProof.tsx, src/styles/social-proof.css</files>
  <action>
**SocialProof.tsx changes:**

Add a static `trustPoints` array above the component (after the `deploysTo` array). Each entry has `label: string` and `body: string`:

```tsx
const trustPoints = [
  {
    label: "Workflow-native reliability",
    body: "Durable background workers and replayable workflows — your jobs survive restarts and infra changes.",
  },
  {
    label: "Unified cloud + self-hosted",
    body: "One navigation system for cloud-provisioned and self-hosted nodes — no context switching between control planes.",
  },
  {
    label: "Deep credential lifecycle",
    body: "SSH keys, vault-backed secrets, and access-key rotation — not only environment variables.",
  },
  {
    label: "Rollout controls by role",
    body: "Gate deployments by org or role so teams can run controlled launches without feature flag infrastructure.",
  },
];
```

Inside the `SocialProof` component, wrap the existing content in a new outer `<div className="social-proof-content">` and prepend a trust-points row before it:

```tsx
export function SocialProof() {
  return (
    <section className="social-proof">
      <div className="container">
        <div className="trust-points-grid">
          {trustPoints.map((tp) => (
            <div key={tp.label} className="trust-point">
              <span className="trust-point-label">{tp.label}</span>
              <p className="trust-point-body">{tp.body}</p>
            </div>
          ))}
        </div>
        <div className="social-proof-inner">
          {/* existing groups unchanged */}
          <div className="social-proof-group">
            <span className="social-proof-label">Built on</span>
            <div className="social-proof-items">
              {builtOn.map((t) => (
                <span key={t.name} className="social-proof-item">
                  {t.icon}
                  {t.name}
                </span>
              ))}
            </div>
          </div>
          <div className="social-proof-divider" />
          <div className="social-proof-group">
            <span className="social-proof-label">Deploys to</span>
            <div className="social-proof-items">
              {deploysTo.map((t) => (
                <span key={t.name} className="social-proof-item">
                  {t.icon}
                  {t.name}
                </span>
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
```

**social-proof.css changes:**

Add the following new classes at the end of the file (before the closing media query block, or after the existing `@media (max-width: 768px)` block):

```css
/* Trust talking-points grid — above the logos strip */
.trust-points-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1rem 1.5rem;
  padding: 2rem 0 1.75rem;
  border-bottom: 1px solid var(--border-subtle);
  margin-bottom: 1.75rem;
}

.trust-point {
  display: flex;
  flex-direction: column;
  gap: 0.375rem;
}

.trust-point-label {
  font-family: var(--font-heading);
  font-size: 0.8125rem;
  font-weight: 700;
  color: var(--text-primary);
  letter-spacing: 0.01em;
}

.trust-point-body {
  font-size: 0.8125rem;
  line-height: 1.6;
  color: var(--text-secondary);
  margin: 0;
}

@media (max-width: 900px) {
  .trust-points-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 540px) {
  .trust-points-grid {
    grid-template-columns: 1fr;
  }
}
```

Note: The existing `.social-proof-inner` flex layout still applies to the logos strip. The `container` div wrapping both sections ensures consistent max-width alignment. If the existing `social-proof-inner` class already serves as the container, move it to wrap only the logos strip (as shown in the JSX above) rather than the whole section — the key constraint is that `.trust-points-grid` must be a sibling of `.social-proof-inner`, both inside `.container`.
  </action>
  <verify>
    <automated>grep -c "trust-point" /Users/davidpeter/workspace/type-driven.com/nupaas-landing/src/components/SocialProof.tsx | xargs -I{} test {} -ge 4 &amp;&amp; echo "PASS: trust-point classes present" || echo "FAIL"</automated>
    <automated>grep -n "trust-points-grid" /Users/davidpeter/workspace/type-driven.com/nupaas-landing/src/styles/social-proof.css &amp;&amp; echo "PASS: CSS class defined"</automated>
    <automated>grep -c "label:" /Users/davidpeter/workspace/type-driven.com/nupaas-landing/src/components/SocialProof.tsx | xargs -I{} test {} -eq 4 &amp;&amp; echo "PASS: 4 trust points defined" || echo "FAIL: trust point count mismatch"</automated>
    <automated>cd /Users/davidpeter/workspace/type-driven.com/nupaas-landing &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -20</automated>
  </verify>
  <done>
    - `trustPoints` array has exactly 4 entries covering: workflow reliability, unified model, credential lifecycle, rollout controls
    - `trust-points-grid` renders above the logos strip inside the same `section.social-proof`
    - Each trust point renders a bold label (`trust-point-label`) and a short body (`trust-point-body`)
    - CSS grid is 4-column on desktop, 2-column at 900px, 1-column at 540px
    - Logos strip (Built on / Deploys to) is visually unchanged — its flex layout still applies
    - TypeScript compiles with no errors introduced by these files
  </done>
</task>

</tasks>

<verification>
After both tasks are complete, verify the full phase:

1. Run `npx tsc --noEmit` from the project root — zero new errors.
2. Run `npm run dev` and open the browser:
   - Sovereignty section: card 3 heading reads "Designed for GDPR-conscious teams, Gaia-X aligned". Two badges visible (GDPR, Gaia-X aligned). No lock-icon "SOC 2 ready" badge.
   - SocialProof section: four trust talking-point labels visible above the Built on / Deploys to logos strip.
3. Resize to 768px — trust grid collapses to 2 columns, logos strip stacks vertically.
4. Search the built output for the string "SOC 2" — must return zero matches in Sovereignty.tsx.
</verification>

<success_criteria>
- SOV-01: Sovereignty card 3 heading is "Designed for GDPR-conscious teams, Gaia-X aligned"
- SOV-02: Card 3 body contains "audit-friendly controls" — no Gaia-X credential qualification language
- SOV-03: No SOC 2 badge element exists in Sovereignty.tsx
- SOV-04: Gaia-X badge text reads "Gaia-X aligned"
- TRUST-01: SocialProof renders exactly 4 trust talking points above logos strip, covering the four specified themes
- TypeScript: `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-trust-signals/02-01-SUMMARY.md` using the summary template at `@/Users/davidpeter/.claude/get-shit-done/templates/summary.md`.
</output>
